---
title: Home
description: Hey! 👋 thanks for stopping by.
layout: page
eleventyExcludeFromCollections: true

---
{% import "home-macros.njk" as home %}

{{ home.aboutMe() }}

<p>This is my <a href="{% link_to 'digital-garden' %}">digital garden</a>.</p>

<h3 class="page-category">Shared</h3>
<p>Things I have posted elsewhere (because they are likely interesting to other folks).</p>

{%- for item in collections.linkedTo %}
{{ home.item(item.url, item.data) }}
{%- endfor %}

<h3 class="page-category">In Progress</h3>

{%- for item in collections.wip %}
{{ home.item(item.url, item.data) }}
{%- endfor %}

<h3 class="page-category">Everything Else</h3>

{%- for item in collections.updated %}
{{ home.item(item.url, item.data) }}
{%- endfor %}
